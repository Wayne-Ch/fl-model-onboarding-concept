{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Wayne-Ch/fl-model-onboarding-concept/contracts/recipe-attempt.schema.json",
  "title": "Recipe Attempt Store Records",
  "description": "Typed immutable records persisted by RecipeAttemptStore for generated candidates, attempts, and verified promotions.",
  "type": "object",
  "oneOf": [
    {
      "$ref": "#/$defs/generated_recipe_record"
    },
    {
      "$ref": "#/$defs/recipe_attempt"
    },
    {
      "$ref": "#/$defs/verified_recipe_record"
    }
  ],
  "$defs": {
    "hex40": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "hex64": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "iso_datetime": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}T.+$"
    },
    "recipe_status": {
      "type": "string",
      "enum": [
        "experimental",
        "verified",
        "blocked"
      ]
    },
    "attempt_state": {
      "type": "string",
      "enum": [
        "generated",
        "running",
        "succeeded",
        "failed",
        "cancelled"
      ]
    },
    "attempt_gate_name": {
      "type": "string",
      "enum": [
        "mobius_build",
        "olive_optimize",
        "onnx_validation",
        "ort_validation",
        "oga_validation",
        "fl_sdk_inference",
        "quality_validation"
      ]
    },
    "attempt_gate_status": {
      "type": "string",
      "enum": [
        "passed",
        "failed"
      ]
    },
    "attempt_failure_classification": {
      "type": "string",
      "enum": [
        "gate_failed",
        "validation_failed",
        "interrupted",
        "cancelled",
        "internal_error"
      ]
    },
    "gate_check": {
      "type": "object",
      "required": [
        "passed",
        "evidence"
      ],
      "properties": {
        "passed": {
          "type": "boolean",
          "const": true
        },
        "evidence": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "promotion_evidence": {
      "type": "object",
      "required": [
        "mobius_build",
        "olive_optimize",
        "onnx_validation",
        "ort_validation",
        "oga_validation",
        "fl_sdk_inference",
        "quality_validation"
      ],
      "properties": {
        "mobius_build": {
          "$ref": "#/$defs/gate_check"
        },
        "olive_optimize": {
          "$ref": "#/$defs/gate_check"
        },
        "onnx_validation": {
          "$ref": "#/$defs/gate_check"
        },
        "ort_validation": {
          "$ref": "#/$defs/gate_check"
        },
        "oga_validation": {
          "$ref": "#/$defs/gate_check"
        },
        "fl_sdk_inference": {
          "$ref": "#/$defs/gate_check"
        },
        "quality_validation": {
          "$ref": "#/$defs/gate_check"
        }
      },
      "additionalProperties": false
    },
    "attempt_gate_result": {
      "type": "object",
      "required": [
        "sequence",
        "gate",
        "status",
        "evidence_ref",
        "metrics_ref",
        "started_utc",
        "finished_utc"
      ],
      "properties": {
        "sequence": {
          "type": "integer"
        },
        "gate": {
          "$ref": "#/$defs/attempt_gate_name"
        },
        "status": {
          "$ref": "#/$defs/attempt_gate_status"
        },
        "evidence_ref": {
          "type": "string"
        },
        "metrics_ref": {
          "type": [
            "string",
            "null"
          ]
        },
        "started_utc": {
          "$ref": "#/$defs/iso_datetime"
        },
        "finished_utc": {
          "$ref": "#/$defs/iso_datetime"
        }
      },
      "additionalProperties": false
    },
    "attempt_failure": {
      "type": "object",
      "required": [
        "classification",
        "stage",
        "message",
        "evidence_refs",
        "source_owner",
        "next_action"
      ],
      "properties": {
        "classification": {
          "$ref": "#/$defs/attempt_failure_classification"
        },
        "stage": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "evidence_refs": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "source_owner": {
          "type": "string"
        },
        "next_action": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "generated_recipe_record": {
      "type": "object",
      "required": [
        "record_type",
        "recipe_fingerprint",
        "schema_version",
        "recipe_status",
        "model_id",
        "revision_sha",
        "requested_device",
        "requested_precision",
        "compiler_version",
        "capability_fingerprint",
        "toolchain_fingerprint",
        "profile_fingerprint",
        "canonical_json",
        "created_utc"
      ],
      "properties": {
        "record_type": {
          "type": "string",
          "const": "generated_recipe_record"
        },
        "recipe_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "schema_version": {
          "type": "string",
          "const": "1.0.0"
        },
        "recipe_status": {
          "$ref": "#/$defs/recipe_status"
        },
        "model_id": {
          "type": "string"
        },
        "revision_sha": {
          "$ref": "#/$defs/hex40"
        },
        "requested_device": {
          "type": "string"
        },
        "requested_precision": {
          "type": "string"
        },
        "compiler_version": {
          "type": "string"
        },
        "capability_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "toolchain_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "profile_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "canonical_json": {
          "type": "string"
        },
        "created_utc": {
          "$ref": "#/$defs/iso_datetime"
        }
      },
      "additionalProperties": false
    },
    "recipe_attempt": {
      "type": "object",
      "required": [
        "record_type",
        "attempt_id",
        "idempotency_key",
        "request_fingerprint",
        "recipe_fingerprint",
        "model_id",
        "revision_sha",
        "requested_device",
        "requested_precision",
        "compiler_version",
        "capability_fingerprint",
        "toolchain_fingerprint",
        "profile_fingerprint",
        "created_utc",
        "finished_utc",
        "state",
        "gate_results",
        "failure"
      ],
      "properties": {
        "record_type": {
          "type": "string",
          "const": "recipe_attempt"
        },
        "attempt_id": {
          "type": "string"
        },
        "idempotency_key": {
          "type": "string"
        },
        "request_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "recipe_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "model_id": {
          "type": "string"
        },
        "revision_sha": {
          "$ref": "#/$defs/hex40"
        },
        "requested_device": {
          "type": "string"
        },
        "requested_precision": {
          "type": "string"
        },
        "compiler_version": {
          "type": "string"
        },
        "capability_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "toolchain_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "profile_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "created_utc": {
          "$ref": "#/$defs/iso_datetime"
        },
        "finished_utc": {
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "$ref": "#/$defs/attempt_state"
        },
        "gate_results": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/attempt_gate_result"
          }
        },
        "failure": {
          "type": [
            "object",
            "null"
          ],
          "required": [
            "classification",
            "stage",
            "message",
            "evidence_refs",
            "source_owner",
            "next_action"
          ],
          "properties": {
            "classification": {
              "$ref": "#/$defs/attempt_failure_classification"
            },
            "stage": {
              "type": "string"
            },
            "message": {
              "type": "string"
            },
            "evidence_refs": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "source_owner": {
              "type": "string"
            },
            "next_action": {
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "verified_recipe_record": {
      "type": "object",
      "required": [
        "record_type",
        "verified_fingerprint",
        "source_recipe_fingerprint",
        "attempt_id",
        "schema_version",
        "model_id",
        "revision_sha",
        "requested_device",
        "requested_precision",
        "compiler_version",
        "capability_fingerprint",
        "toolchain_fingerprint",
        "profile_fingerprint",
        "promoted_utc",
        "promotion_evidence",
        "canonical_json"
      ],
      "properties": {
        "record_type": {
          "type": "string",
          "const": "verified_recipe_record"
        },
        "verified_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "source_recipe_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "attempt_id": {
          "type": "string"
        },
        "schema_version": {
          "type": "string",
          "const": "1.0.0"
        },
        "model_id": {
          "type": "string"
        },
        "revision_sha": {
          "$ref": "#/$defs/hex40"
        },
        "requested_device": {
          "type": "string"
        },
        "requested_precision": {
          "type": "string"
        },
        "compiler_version": {
          "type": "string"
        },
        "capability_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "toolchain_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "profile_fingerprint": {
          "$ref": "#/$defs/hex64"
        },
        "promoted_utc": {
          "$ref": "#/$defs/iso_datetime"
        },
        "promotion_evidence": {
          "$ref": "#/$defs/promotion_evidence"
        },
        "canonical_json": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
